import { ArgsTable, Canvas, Meta, Story } from "@storybook/addon-docs";

import { Flex, FlexProps } from "@components/system";

<Meta
  title="Utility Components/Flex"
  parameters={{
    viewMode: "docs",
    previewTabs: {
      canvas: { hidden: true },
    },
  }}
  component={Flex}
/>

# Flex

The `Flex` Component is an extension of the `Box` Component and adds BorderProps

<Story name="Flex">
  <Flex
    width="12rem"
    height="12rem"
    bg="white"
    border="1px solid black"
    borderRadius="1rem"
  />
</Story>

<ArgsTable of={FlexProps} />
